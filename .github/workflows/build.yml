name: build

on:
  push:
    branches: [main]
  pull_request:

jobs:
  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"
      - name: Restore
        run: dotnet restore Vestigium.Converters.slnx
      - name: Build
        run: dotnet build Vestigium.Converters.slnx -c Release --no-restore
      - name: Test
        run: dotnet test src/Vestigium.Converters.Tests/Vestigium.Converters.Tests.csproj -c Release --no-build --logger "trx;LogFileName=test-results.trx"
